import { Components } from '@mui/material';

export const containerConfigs: Components['MuiContainer'] = {
  styleOverrides: {
    root: {
      paddingTop: 40,
      paddingBottom: 40,
    },
  },
};
